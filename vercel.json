{
    "version": 2,
    "buildCommand": "next build",
    "build": [
        {
            "src": "*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "command": "npm run build"
}
